class EventModel {
  String id;
  String title;
  DateTime dateTime;
  String artistEnAvant;
  String festivalId;

  EventModel(this.id, this.title, this.dateTime, this.artistEnAvant, this.festivalId);
}
