class FestivalModel {
  final String id;
  final String title;
  final String description;
  final String photoUrl;
  final DateTime dateStart;
  final DateTime dateEnd;

  FestivalModel(this.id, this.title, this.description, this.photoUrl,
      this.dateStart, this.dateEnd);
}
