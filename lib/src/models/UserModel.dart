class UserModel{
  String id;
  String firstname;
  String lastname;
  String username;
  String email;
  bool enabled;

  UserModel(this.id, this.firstname, this.lastname, this.username, this.email,
      this.enabled);
}