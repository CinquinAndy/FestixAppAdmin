class ArtistModel{
  String id;
  String artistName;
  String description;
  String musicStyle;
  String photoUrl;

  ArtistModel(this.id, this.artistName, this.description, this.musicStyle,
      this.photoUrl);
}